 // setup date picker
    var pDate = new Date() ;
    $('#datetimepicker1').datetimepicker(
            {
                format: 'YYYY/MM/DD',

            }
    );
    $('#datetimepicker2').datetimepicker(
            {
                format: 'YYYY/MM/DD',

            }
    )